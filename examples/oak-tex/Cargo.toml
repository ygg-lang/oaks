[package]
name = "oak-tex"
description = "TeX/LaTeX document preparation system parser with support for typesetting commands and macros."
categories = ["parsing"]
keywords = ["parser", "tex", "latex", "typesetting"]
documentation = "https://docs.rs/oak-tex"
readme = "readme.md"
version.workspace = true
authors.workspace = true
homepage.workspace = true
repository.workspace = true
license.workspace = true
edition.workspace = true

[dependencies]
oak-core = { workspace = true }
oak-highlight = { workspace = true }
oak-pretty-print = { workspace = true }
oak-lsp = { workspace = true }
oak-mcp = { workspace = true, features = ["io-std"] }
oak-hover = { workspace = true }
oak-vfs = { workspace = true }
futures = { workspace = true }
serde = { workspace = true, features = ["derive"] }
tokio = { workspace = true, optional = true }

[dev-dependencies]
oak-testing = { workspace = true }

[features]
default = []
std = []

[package.metadata.docs.rs]
all-features = true
