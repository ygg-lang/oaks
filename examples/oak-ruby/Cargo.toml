[package]
name = "oak-ruby"
description = "High-performance incremental Ruby parser for the oak ecosystem with flexible configuration, supporting dynamic programming and rapid application development."
categories = ["parsing"]
keywords = ["parser", "ruby", "ast", "language"]
documentation = "https://docs.rs/oak-ruby"
readme = "readme.md"
version.workspace = true
authors.workspace = true
homepage.workspace = true
repository.workspace = true
license.workspace = true
edition.workspace = true

[dependencies]
oak-core = { workspace = true }
oak-highlight = { workspace = true, optional = true }
oak-pretty-print = { workspace = true, optional = true }

[dev-dependencies]
oak-core = { workspace = true, features = ["testing"] }

walkdir = "2.5"

[features]
default = []
alloc = []

[package.metadata.docs.rs]
all-features = true