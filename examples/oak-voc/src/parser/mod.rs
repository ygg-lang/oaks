/// VOC parser.
pub struct VocParser;

impl VocParser {
    /// Create a new VOC parser.
    pub fn new() -> Self {
        Self
    }
}

/// Element types for the VOC language.
pub mod element_type;
