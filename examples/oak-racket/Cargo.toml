[package]
name = "oak-racket"
version = "0.0.5"
edition = "2021"

[dependencies]
oak-core = { workspace = true }
serde = { workspace = true, optional = true, features = ["derive"] }

[features]
serde = ["oak-core/serde", "dep:serde"]

[dev-dependencies]
oak-testing = { workspace = true }
