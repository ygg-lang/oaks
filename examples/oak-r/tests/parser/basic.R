# 基础表达式
1 + 2 * 3
(1 + 2) * 3
-5 + 10
TRUE && FALSE || TRUE

# 赋值
x <- 42
y = "hello"
z <<- 100
10 -> a

# 函数调用
print("Hello, R!")
sum(1, 2, 3)
plot(x, y, main="Plot")

# 控制流
if (x > 0) {
  print("Positive")
} else {
  print("Non-positive")
}

while (x > 0) {
  x <- x - 1
}

for (i in 1:10) {
  print(i)
}

# 函数定义
my_func <- function(a, b) {
  return(a + b)
}
