# Wolfram AST

Language-specific typed projection over the Wolfram CST (`GreenTree` / `RedNode`).

Provides `TypedNode` casts plus stable accessors (`lhs` / `rhs` / `operator` / `head` / `arguments` / `span`) for SXO Form lowering. This is **not** a generic AST and is **not** AthenaIR.
