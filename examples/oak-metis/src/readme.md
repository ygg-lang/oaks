# oak-metis

Metis island language front-end for Oaks: lexer / parser / typed AST.

Source files use the **`.metis`** suffix. Keywords declare (`island`, `node`, `axiom`, `theorem`, …). Actions are static `Module::fn` paths. This crate does **not** own the relation graph kernel.
