[package]
name = "oak-scss"
description = "High-performance incremental SCSS parser for the oak ecosystem with flexible configuration, supporting CSS preprocessing and styling."
categories = ["parsing"]
keywords = ["parser", "scss", "css", "stylesheet"]
documentation = "https://docs.rs/oak-scss"
readme = "readme.md"
version.workspace = true
authors.workspace = true
homepage.workspace = true
repository.workspace = true
license.workspace = true
edition.workspace = true

[dependencies]
oak-core = { workspace = true }
oak-highlight = { workspace = true, optional = true }
oak-pretty-print = { workspace = true, optional = true }


[dev-dependencies]
oak-core = { workspace = true, features = ["testing"] }


[features]
default = [ "oak-highlight"]


[package.metadata.docs.rs]
all-features = true