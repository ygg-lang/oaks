[package]
name = "oak-php"
description = "High-performance incremental PHP parser for the oak ecosystem with flexible configuration."
categories = ["parsing"]
keywords = ["parser", "php", "web-development", "server-side"]
documentation = "https://docs.rs/oak-php"
readme = "readme.md"
version.workspace = true
authors.workspace = true
homepage.workspace = true
repository.workspace = true
license.workspace = true
edition.workspace = true

[dependencies]
oak-core = { workspace = true }
oak-highlight = { workspace = true, optional = true }
oak-pretty-print = { workspace = true, optional = true }

[dev-dependencies]
oak-core = { workspace = true, features = ["testing"] }


[features]
default = []
alloc = []

[package.metadata.docs.rs]
all-features = true