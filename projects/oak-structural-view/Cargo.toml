[package]
name = "oak-structural-view"
version.workspace = true
authors.workspace = true
description = "AST-based document structure and symbol tree generation for the Oak framework."
edition.workspace = true
license.workspace = true
repository = "https://github.com/ygg-lang/oaks/tree/master/projects/oak-structural-view"
categories = ["development-tools"]
keywords = ["structure", "outline", "ast", "symbols"]

[dependencies]
oak-core = { workspace = true, features = ["serde"] }
serde = { workspace = true }
